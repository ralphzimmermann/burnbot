# Frontend environment variables

# Base URL of the backend API
# Leave empty in production (same-origin). For dev with Vite proxy, leave empty as well.
# To bypass proxy in dev, uncomment:
# VITE_API_BASE_URL=http://127.0.0.1:8000


