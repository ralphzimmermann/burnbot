// Tailwind v4 uses the CSS-based config. This file is no longer required,
// but we keep it minimal to avoid tool errors.
export default {
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
}


